# テンキ爺ラジオ — Claude Design 프레젠테이션 지침서 v2

> 이 문서를 Claude Design에 첨부하여 PPT를 생성하세요.
> 슬라이드 수: **15장** / 대상: 처음 보는 사람 / 언어: 한국어 (일본어 고유명사 병기)

---

## 디자인 방향

- **배경**: 짙은 네이비(#0D1B2A)
- **포인트 컬러**: 앰버 골드(#F5A623) — 헤드라인 강조, 아이콘, 슬라이드 번호
- **보조 컬러**: 연한 청록(#4DD9AC) — 코드 블록, QA/검증 관련 요소
- **경고/빌런 컬러**: 선명한 빨강(#FF4D4D) — 빌런 아키타입, 실패 케이스
- **폰트**: 헤드라인 Bold sans-serif / 본문 Regular / 코드 monospace
- **반복 모티프**: 전파선(라디오), 회로기판, 마이크, 로봇 눈(단안)
- **슬라이드 번호**: 우측 하단, 앰버 골드

---

## 슬라이드 1 — 타이틀

**헤드라인 (크게):**
```
テンキ爺の電波局
```

**서브타이틀:**
AI 전자동 유튜브 라디오 생성 시스템

**한 줄 임팩트 설명 (중앙 강조 박스):**
> 주제 3개를 입력하면, 30분짜리 유튜브 영상이 완성됩니다.  
> 대본부터 이미지 · 음성 · 영상 편집 드래프트까지 — 사람 개입 없이.

**하단 배지 (작은 텍스트):**
Claude · Gemini · ElevenLabs · CapCut

**비주얼 제안:** 다크 배경에 빈티지 라디오 + 회로기판 일러스트. 전파선이 방사형으로 퍼지는 구도.

---

## 슬라이드 2 — "얼마나 대단한가?" 숫자로 보기

**헤드라인:** 숫자로 보는 자동화 규모

**중앙 대형 숫자 카드 (4개):**

| **1** | **9** | **3** | **50+** |
|---|---|---|---|
| 개의 명령어 | 개의 전문 AI 에이전트 | 개의 QA 레이어 | 개의 씬 이미지 |
| `/go-radio` 하나로 전체 실행 | 각자 역할이 완전히 분리됨 | 대본·비주얼·TTS 전 구간 검증 | 에피소드당 자동 생성 |

**두 번째 줄 숫자 카드 (3개):**

| **7** | **3** | **∞** |
|---|---|---|
| 개의 목소리 | 개의 오디오 청크 | 회 실행해도 실수 반복 안 함 |
| 화자 나이·성별별 자동 배정 | 방송 구조에 맞춰 자동 분할 | 자가 학습 루프가 실수를 누적 학습 |

**하단 강조 문구:**
> 이 모든 것이 Node.js + Claude API + Gemini API + ElevenLabs API 위에서 돌아갑니다.

---

## 슬라이드 3 — DJ 테나키지는 누구인가

**헤드라인:** テンキ爺（てんきじい） — 고물 로봇 DJ

**중앙 캐릭터 설명 (좌우 2단):**

**왼쪽 — 설정:**
- 폐품 수거장에서 주워진 8비트 CPU, 256KB 메모리의 녹슨 라디오 DJ 로봇
- 공식 이름: **テンキ爺** — 「鉄男」「テツオ」등 어떤 변형도 전면 금지
- 외형: 사각 두상 + 단안(單眼) 발광 + 구부러진 안테나 + 아날로그 다이얼

**오른쪽 — 화법:**
- 1인칭: 「ワシ」만 사용
- 청취자 호칭: 「コノヤロー」「電波泥棒」「このバカ野郎」
- 자기비하 키워드: ポンコツ / 8ビット / 256KB / 廃品回収 / 錆

**중앙 강조 박스 (인용):**
> "笑いながら刺す（웃으면서 찌른다）"  
> 팩트가 너무 정확해서 웃음이 나오는 한 마디.  
> 하지만 2초 후 "아... 진짜네" 가 되어야 한다.  
> — テンキ爺 페르소나 규칙 중

**롤모델:** ビートたけしのオールナイトニッポン  
→ 짧게 끊지 말고 옛날 썰을 길게. 독설 → 비유 → 투박한 위로의 3단 구조.

---

## 슬라이드 4 — 전체 파이프라인 아키텍처

**헤드라인:** 전체 파이프라인 — 한눈에

**중앙 흐름도 (세로 또는 가로 플로우차트):**

```
[ 사용자: /go-radio 주제1 주제2 주제3 ]
              ↓
┌─────────────────────────────────────┐
│  STEP 00A  트렌드 수집               │  Yahoo Japan + NHK RSS 자동 크롤링
│  STEP 00B  에피소드 플래너           │  빌런·키워드·서사 구조 사전 결정 → 계약서 발행
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  STEP 01   대본 작가                 │  사연 3편 생성 (800~1000자/편)
│  STEP 01-QA  대본 QA                │  프로그래매틱 + LLM 가중치 채점 → 85점 커트라인
└─────────────────────────────────────┘
              ↓ (QA 실패 시 최대 3회 재생성)
┌─────────────────────────────────────┐
│  STEP 02   DJ 멘트 생성              │  テンキ爺 인격으로 오프닝·리액션·엔딩 작성
│  STEP 08*  즉문즉답 코너 (--qa 옵션) │  황당 질문 5개 × 츤데레 독설
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  STEP 03   캐스팅 디렉터             │  캐릭터 시드 프롬프트 고정
│  STEP 04A  스토리보드 디렉터         │  4~6초 씬 flat 배열로 방송 분해
│  STEP 04B  비주얼 QA                │  LLM 채점 + API 규격 프로그래매틱 검증
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  STEP 05   이미지 생성               │  Gemini 2.5 Flash Image, 씬별 16:9 PNG
│  STEP 07   TTS 오디오                │  ElevenLabs, 화자별 자동 배정, 3청크 분할
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  STEP 06   CapCut 드래프트 조립      │  타임라인 + 오디오 + SFX 자동 배치
│  STEP 99   자가 학습 루프            │  실수 누적 → 다음 에피소드에 주입
└─────────────────────────────────────┘
              ↓
   [ 완성: 유튜브 업로드 직전 상태 ]
```

**우측 범례:**
- 🟡 생성 단계
- 🟢 QA/검증 단계
- 🔵 조립/출력 단계
- 🔴 자가 학습 단계

---

## 슬라이드 5 — 에피소드 계약서 시스템 (핵심 설계)

**헤드라인:** 에피소드 계약서 — 모든 에이전트를 구속하는 창작 헌법

**설명 (상단):**
파이프라인이 시작되면 가장 먼저 **플래너(Planner)**가 실행됩니다.  
플래너는 대본이 쓰이기 *전에* 모든 규칙을 결정하고, 그 결과를 JSON 계약서로 발행합니다.  
이후 모든 AI 에이전트는 이 계약서를 벗어날 수 없습니다.

**계약서가 결정하는 것들 (체크리스트 형태):**

| 항목 | 설명 |
|---|---|
| ✅ 에피소드별 필수 키워드 | 대본에 반드시 등장해야 할 일본어 핵심 단어 3~5개 |
| ✅ 캐릭터 나이대·성격 힌트 | 시니어 or MZ 사연자 여부, 성격 방향 |
| ✅ 감정 톤 | 苦笑い / 哀愁 / 懐かしさ / ほっこり / 驚き 중 1개 |
| ✅ 금지 이탈 패턴 | 이 테마에서 빠지면 안 되는 방향 2가지 |
| ✅ 서사 구조 (起承転結) | 4단계 스케치 — 대본 작가는 이를 살로 채울 뿐 |
| ✅ テンキ爺 훅 타이밍 | DJ가 끼어들 장면 2곳 사전 지정 |
| ✅ 빌런 배정 결과 | LLM이 임의로 변경 불가 — 플래너 결정값이 강제 적용됨 |

**하단 인용:**
> "LLM에게 창작 자유를 주면 매번 다른 결과가 나온다. 계약서는 그 자유의 범위를 제한하여 품질 예측 가능성을 확보한다."

---

## 슬라이드 6 — 빌런 아키타입 시스템

**헤드라인:** 빌런 설계 — "가해자인데 피해자라고 믿는 사람"

**상단 설명:**
매 방송마다 30% 확률로 빌런 사연자가 등장합니다.  
이 빌런은 악당이 아닙니다. **자신이 억울한 피해자라고 믿으며 사연을 보냅니다.**  
하지만 사연 속 디테일을 읽다 보면 청취자는 깨닫습니다 — "아, 이 사람이 문제구나."

**3가지 아키타입 카드 (3열):**

| 🔴 自業自得型 | 🟠 善意の迷惑型 | 🟡 お門違い型 |
|---|---|---|
| 본인이 씨를 뿌렸는데 억울해함 | 좋은 의도지만 주변에 민폐 | 화살표 방향이 완전히 반대 |
| 아들이 연락을 끊었다고 투고하지만, 읽다 보면 평생 아들을 억압해온 게 드러남 | 가족을 걱정해서 한 일이라고 쓰지만, 과잉간섭·통제로 주변이 지쳐 떠난 상황 | 직장 동료가 왕따시킨다고 쓰지만, 본인의 언동이 직장 분위기를 망쳐왔음이 드러남 |

**핵심 설계 원칙 (강조 박스):**
> `irony_reveal` 필드 — 플래너가 구체적으로 설계하는 "반전 장면"  
> "사연자가 억울하다 쓰지만, 어떤 대사·행동에서 청취자가 진실을 알아채는가"를  
> 계약서에 60자 이내로 사전 명시. 대본 작가는 이 반전을 자연스럽게 심어야 합니다.

**하단:**
빌런 에피소드와 MZ 에피소드는 항상 다른 EP에 배정 (충돌 방지 로직 내장)

---

## 슬라이드 7 — 3중 QA 시스템

**헤드라인:** 3중 QA — "AI가 AI를 검증한다"

**전체 QA 레이어 개요 (상단):**
이 시스템에는 QA가 3곳에 내장되어 있습니다. 각 레이어는 독립적으로 작동하며, 실패 시 자동 재생성을 트리거합니다.

---

**QA Layer 1 — 대본 검증 (2단계 혼합)**

| Stage | 방법 | 검사 항목 | 커트라인 |
|---|---|---|---|
| Stage 1-A | 프로그래매틱 | 대본 분량(700자↑), 필수 키워드 포함 여부, 빌런 플래그 일치 | 하나라도 실패 → 즉시 Fail |
| Stage 1-B | Claude LLM 가중 채점 | 계약 이행[40점] + V3 오디오 태그 규칙[40점] + 페르소나·분량[20점] | **85점** 미달 → Fail |

→ Fail 시: `01_qa_feedback.json` 생성 → 피드백을 다음 재시도에 주입 → 최대 3회

---

**QA Layer 2 — 비주얼 프롬프트 검증 (2단계 혼합)**

| Stage | 방법 | 검사 항목 | 커트라인 |
|---|---|---|---|
| Stage 2-A | Gemini LLM 가중 채점 | 캐릭터 일관성·시니어 제약[40점] + 물리 리얼리즘[30점] + 아트 스타일[30점] | **90점** 미달 → Auto-fix |
| Stage 2-B | 프로그래매틱 API 규격 | 프롬프트 2000자 이하, Midjourney 파라미터 자동 제거, 필수 파라미터 시뮬레이션 | 위반 즉시 수정 |

→ 실패한 씬만 선택적으로 재생성 (통과한 씬 건드리지 않음)

---

**QA Layer 3 — 자가 학습 루프 (99_wrapup)**

파이프라인 마지막에 항상 실행. 이번 실행의 실수를 추출 → Gemini로 중복 제거 + 압축 → `ref_learnings.md` (최대 10개 bullet 유지)  
다음 실행 시 대본 작가·DJ 에이전트가 이 파일을 자동 주입받아 **같은 실수를 반복하지 않는다.**

---

## 슬라이드 8 — 자가 학습 루프 상세

**헤드라인:** AI가 자신의 실수를 기억한다

**흐름 다이어그램 (좌→우):**

```
[이번 실행]
QA Fail 발생
     ↓
01_qa_feedback.json 생성
(구체적 실패 이유 + Actionable 수정 지시)
     ↓
재시도 시 피드백 주입 → 대본 재생성
     ↓
파이프라인 완료
     ↓
99_wrapup 실행
     ↓
Gemini 2.5 Flash: 기존 오답 노트 + 새 실수 병합
중복 제거 + 압축 → 최대 10개 bullet
     ↓
ref_learnings.md 갱신

[다음 실행]
대본 작가 에이전트가 ref_learnings.md 자동 로드
"과거에 이런 실수를 했으니 이번엔 피한다"
```

**실제 누적된 오답 노트 예시 (코드 블록):**
```
- [V3태그] script 필드에 영문 Audio Tag 사연 1편당 3개 이상 필수
- [계약] required_keywords는 단순 삽입 금지 — 서사에 자연스럽게 녹아들어야 함
- [빌런] villain_required=true 에피소드에서 빌런성은 사연자 본인 행동으로 명시적 표현
- [분량] 800字 미만 즉시 Fail — 감정 변화 3단계 + 대화 직접 인용 1회 필수
```

**핵심 포인트:**
> 새로운 실수가 없으면 파일을 그대로 유지 (no-op). 비대해지는 문제 없음.  
> 실수가 있을 때만 Gemini가 압축·병합을 실행.

---

## 슬라이드 9 — 에피소드 격리 & 재시작 가능성

**헤드라인:** EP_ID 격리 — "실패해도 처음부터 다시 시작하지 않는다"

**2단 레이아웃:**

**왼쪽 — EP_ID 격리 구조:**
모든 실행은 고유 ID를 가집니다: `EP_YYYYMMDD_HHMM`  
산출물은 전부 `.output/{EP_ID}/` 하위에 격리됩니다.

```
.output/
├── EP_20260420_1430/   ← 이번 실행
│   ├── manifest.json   ← 파이프라인 상태 추적
│   ├── 01_scripts.json
│   ├── 02_dj_script.json
│   ├── images/SC001.png ...
│   └── audio/chunk_1.mp3 ...
└── EP_20260419_2100/   ← 이전 실행 (그대로 보존)
```

→ 동시에 여러 에피소드 실행 가능. 파일 충돌 원천 차단.

**오른쪽 — manifest 상태 머신:**
파이프라인의 진행 상황을 `manifest.json`이 추적합니다.

```json
{ "stage": "images_done" }
```

| stage 값 | 의미 |
|---|---|
| `init` | 플래너 완료 |
| `scripted` | 대본 완료 |
| `storyboarded` | 스토리보드 완료 |
| `images_done` | 이미지 생성 완료 |
| `audio_done` | TTS 완료 |
| `capcut_done` | 최종 완성 |

→ 중간에 실패해도 완료된 단계는 스킵하고 실패 지점부터 재시작.  
→ 이미 생성된 이미지·오디오는 건드리지 않음 → API 비용 낭비 없음.

---

## 슬라이드 10 — 캐릭터 시각 일관성 시스템

**헤드라인:** 같은 캐릭터가 50개 씬에서 같은 얼굴로 나온다

**문제 설명:**
AI 이미지 생성은 매번 결과가 달라집니다.  
동일 인물이 씬마다 다른 얼굴로 나오면 시청자는 혼란스럽습니다.

**해결 방법 — 캐릭터 시드(Character Seed):**

```
[03 캐스팅 디렉터]
ref_visual_rules.md 규칙 기반으로
각 사연 주인공의 외모를 영어로 상세 고정

예:
"A Japanese woman in her late 60s, 
silver-streaked bob haircut, 
warm brown cardigan, 
gentle but tired expression..."

     ↓ 모든 씬에 동일 시드 삽입

[04 스토리보드]  [05 이미지 생성]
씬별 visual_prompt_en 맨 앞에 character_seed 자동 삽입
```

**DJ テンキ爺 고정 시드 (영구 불변):**
```
"A battered retro tin robot DJ (Tenki-jii) at a vintage Showa-era 
radio desk, square boxy head with cracked paint and rust spots, 
single glowing mono-eye, bent antennae, faded red chest panel 
with analog dials, warm amber vacuum tube glow..."
```

**자동 처리 규칙:**
- **FLASHBACK 씬**: 젊은 시절 묘사 modifier 자동 삽입 ("younger version in their 20s-30s, youthful face...")
- **디바이스 방향**: 스마트폰·사진 화면은 반드시 캐릭터 시선 방향 (뒷판 노출 금지)

---

## 슬라이드 11 — TTS 오디오 시스템

**헤드라인:** 7개의 목소리 — 자동으로 배정된다

**설명:**
ElevenLabs API로 생성되는 TTS는 화자의 나이·성별에 따라 자동으로 다른 Voice ID를 사용합니다.

**Voice ID 배정 테이블:**

| 화자 유형 | Voice ID |
|---|---|
| 🤖 DJ テンキ爺 | 고정 (m0Fo0JrIVm57nweV2EuR) |
| 👴 노년 남성 (60+) | 자동 배정 |
| 🧔 중년 남성 (40~59) | 자동 배정 |
| 👦 청년 남성 (~39) | 자동 배정 |
| 👵 노년 여성 (60+) | 자동 배정 |
| 👩 중년 여성 (40~59) | 자동 배정 |
| 👧 청년 여성 (~39) | 자동 배정 |

**방송 3청크 분할:**

```
chunk_1.mp3 ─── 오프닝 → EP1 사연 → EP1 DJ 리액션 → EP1 트랜지션
chunk_2.mp3 ─── EP2 사연 → EP2 DJ 리액션 → EP2 트랜지션
chunk_3.mp3 ─── EP3 사연 → EP3 DJ 리액션 → (QA 코너) → 엔딩
```

**자동 전처리:**
생성 전 스크립트에서 오염 텍스트(일본어 지문 `[溜息]`, SSML `<break/>`) 자동 탐지·제거.  
FFmpeg로 청크 간 1.5초 묵음 자동 삽입. (FFmpeg 없을 시 폴백 모드 자동 전환)

---

## 슬라이드 12 — CapCut 드래프트 자동 조립

**헤드라인:** 영상 편집 소프트웨어까지 자동으로 준비된다

**설명:**
`run_06_capcut_builder.mjs`는 단순히 파일을 만드는 것이 아닙니다.  
CapCut이 바로 열 수 있는 **프로젝트 파일(draft_content.json)**을 생성합니다.

**자동 계산 및 조립 내용:**

| 처리 항목 | 방법 |
|---|---|
| 씬별 영상 길이 | TTS timestamps.json 기반 sentence boundary 계산 |
| 오디오 anchor 계산 | 각 청크의 파이프라인 내 절대 위치 계산 |
| Ken Burns 효과 | 정적 이미지에 scale-only 줌 효과 자동 적용 |
| 이펙트·스티커 | EP_MAP의 story_end 기준 자동 삽입 |
| 오디오 트랙 | TTS + BGM + radio_noise + pc_click SFX 멀티 트랙 |
| SRT 자막 | timestamps 기반 자막 파일 자동 생성 |

**사람이 해야 할 일:**

| ✅ 자동 | ⬜ 수동 |
|---|---|
| 타임라인 씬 배치 | BGM 최종 선택 |
| 오디오 동기화 | 색보정 |
| 자막 타이밍 | 썸네일 제작 |
| 유튜브 메타데이터(제목·설명·태그) 생성 | 유튜브 업로드 클릭 |

---

## 슬라이드 13 — 에이전트 간 통신 설계

**헤드라인:** 에이전트들은 어떻게 소통하는가

**핵심 원칙 (상단):**
9개 에이전트는 서로 직접 호출하지 않습니다.  
**파일 포인터(manifest.json)** 만을 통해 소통합니다.

**왜 이 설계인가 (3개 이유 카드):**

| 📦 경량 통신 | 🔄 재시작 가능 | 🔍 디버깅 용이 |
|---|---|---|
| 132KB 원본 JSON 대신 경량 summary 뷰 사용 (04_storyboard_summary.json) | 어느 에이전트든 manifest의 stage를 읽어 자신이 실행 필요한지 판단 | 모든 중간 산출물이 파일로 남아 문제 발생 지점 즉시 특정 가능 |

**규칙 주입 방식 (단일 출처 원칙):**

```
대본 작가  →  ref_script_rules.md만 로드
DJ 에이전트 →  ref_persona_rules.md만 로드
비주얼 QA  →  ref_visual_rubric.md만 로드
TTS 에이전트 → ref_tts_v3_core.md만 로드
```

→ 각 에이전트가 필요한 규칙만 선택적으로 주입.  
→ 규칙 수정은 단 하나의 파일만 고치면 파이프라인 전체에 반영.

---

## 슬라이드 14 — 기술 스택

**헤드라인:** 기술 스택

**3열 레이아웃:**

**AI 레이어**

| 역할 | 모델 | 이유 |
|---|---|---|
| 에피소드 플래너 | Claude Haiku 4.5 | 계약서 생성 — 빠르고 저렴 |
| 대본 작가 + QA | Claude Sonnet 4.6 | 고품질 일본어 창작 |
| DJ 멘트 생성 | Gemini 2.5 Pro | 장문 페르소나 유지 |
| 비주얼 QA + 번역 | Gemini 2.5 Flash | 빠른 검증 + 비용 효율 |
| 이미지 생성 | Gemini 2.5 Flash Image | 16:9 씬 이미지 |
| TTS | ElevenLabs API (V3) | 자연스러운 일본어 음성 |

**런타임 & 인프라**

- Node.js 18+ (ESM 모듈)
- Express.js — 웹 대시보드 서버
- Server-Sent Events (SSE) — 실시간 파이프라인 로그
- FFmpeg — 오디오 청크 concat + 묵음 삽입
- CapCut JSON 드래프트 직접 생성 (공식 API 없이 역공학)

**API 키 구성**
```
ANTHROPIC_API_KEY   — Claude (대본 + QA)
GEMINI_API_KEY      — 이미지 + 비주얼 QA + 번역
ELEVENLABS_API_KEY  — TTS
```

---

## 슬라이드 15 — 마무리: 실행과 완성물

**헤드라인:** 하나의 명령, 완성된 유튜브 영상 패키지

**중앙 명령어 박스 (크게, 앰버 골드 테두리):**
```bash
/go-radio                               # 트렌드 자동 수집
/go-radio 노후자금 황혼이혼 스마트폰   # 주제 직접 지정
/go-radio 노후자금 황혼이혼 스마트폰 --qa  # Q&A 코너 포함
```

**완성물 체크리스트 (2열):**

| 산출물 | 파일 |
|---|---|
| 🎭 일본어 사연 대본 3편 | `01_scripts.json` |
| 🎙️ DJ 전체 방송 스크립트 | `02_dj_script.json` |
| 🎨 캐릭터 시드 프롬프트 | `03_character_prompts.json` |
| 🎬 씬 스토리보드 | `04_storyboard.json` |
| 🖼️ 씬별 이미지 PNG (50+장) | `images/SC*.png` |
| 🔊 TTS 오디오 3청크 + 타임스탬프 | `audio/chunk_*.mp3` |
| 📽️ CapCut 프로젝트 드래프트 | `capcut/draft_content.json` |
| 📝 SRT 자막 파일 | `audio/*.srt` |
| 📊 유튜브 메타데이터 | 제목·설명·태그 자동 생성 |
| 🧠 자가 학습 누적 | `ref_learnings.md` 갱신 |

**최하단 마무리 문구 (중앙, 큰 텍스트):**
> "녹슨 로봇이 매주 방송을 만든다."

**작은 서브 텍스트:**
テンキ爺の電波局 — Fully automated. Deeply human.

---

## 부록 A — 슬라이드별 핵심 메시지 요약

| # | 슬라이드 | 전달할 한 가지 |
|---|---|---|
| 1 | 타이틀 | 주제 3개 → 유튜브 완성 |
| 2 | 숫자 | 규모를 수치로 실감 |
| 3 | DJ 캐릭터 | 왜 이 캐릭터인가 |
| 4 | 전체 파이프라인 | 큰 그림 |
| 5 | 에피소드 계약서 | 창작을 어떻게 제어하는가 |
| 6 | 빌런 아키타입 | 심리적 깊이가 설계됨 |
| 7 | 3중 QA | AI가 AI를 검증 |
| 8 | 자가 학습 루프 | 실수를 기억함 |
| 9 | EP_ID 격리 | 안전하고 재시작 가능 |
| 10 | 캐릭터 시각 일관성 | 50장이 같은 얼굴 |
| 11 | TTS | 7가지 목소리 자동 배정 |
| 12 | CapCut 조립 | 영상 편집도 자동 |
| 13 | 에이전트 통신 | 어떻게 9개가 협력하는가 |
| 14 | 기술 스택 | 구체적 기술 명세 |
| 15 | 완성물 | 무엇이 나오는가 |

---

## 부록 B — 설계 철학 5원칙 (슬라이드 추가 또는 배경 소개용)

**① 창작의 결정론화**
LLM에게 무제한 자유를 주면 매번 다른 품질이 나온다. 에피소드 계약서가 창작의 범위를 사전에 결정하여 품질을 예측 가능하게 만든다.

**② AI는 검증자이기도 하다**
생성한 에이전트와 다른 에이전트가 QA한다. Claude가 대본을 쓰면 Claude가 다시 채점한다. 자기 결과를 자기가 비판적으로 보는 루프가 품질을 높인다.

**③ 실수는 자산이다**
QA가 실패하면 그 이유가 기록되어 다음 실행에 주입된다. 실수가 쌓일수록 시스템이 좋아진다. 오답 노트가 영구 메모리 역할을 한다.

**④ 멱등성(Idempotency) — 다시 실행해도 안전하다**
이미 완료된 단계는 건드리지 않는다. 이미지가 있으면 재생성하지 않는다. 중단 후 재시작해도 완료한 만큼 보존된다.

**⑤ 단일 출처 원칙**
DJ 규칙은 `ref_persona_rules.md` 한 곳에만 있다. 대본 규칙은 `ref_script_rules.md` 한 곳에만 있다. 규칙을 바꾸려면 파일 하나만 고치면 된다.
